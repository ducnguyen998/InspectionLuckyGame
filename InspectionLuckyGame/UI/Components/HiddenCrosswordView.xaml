<UserControl x:Class="InspectionLuckyGame.UI.Components.HiddenCrosswordView"
             xmlns="http://schemas.microsoft.com/winfx/2006/xaml/presentation"
             xmlns:x="http://schemas.microsoft.com/winfx/2006/xaml"
             xmlns:mc="http://schemas.openxmlformats.org/markup-compatibility/2006" 
             xmlns:d="http://schemas.microsoft.com/expression/blend/2008" 
             xmlns:local="clr-namespace:InspectionLuckyGame.UI.Components"
             mc:Ignorable="d" >
    <Grid Width="1340" Background="#00000000">
        <StackPanel Orientation="Vertical" VerticalAlignment="Top">
            <ItemsControl ItemsSource="{Binding HiddenCardList}" Margin="0,30,0,0">
                <ItemsControl.ItemTemplate>
                    <DataTemplate DataType="{x:Type local:HiddenCardViewmodel}">
                        <local:HiddenCard/>
                    </DataTemplate>
                </ItemsControl.ItemTemplate>
                <ItemsControl.ItemsPanel>
                    <ItemsPanelTemplate>
                        <UniformGrid Columns="{Binding HiddenCardList.Count}"></UniformGrid>
                    </ItemsPanelTemplate>
                </ItemsControl.ItemsPanel>
            </ItemsControl>
            <TextBlock Style="{StaticResource MaterialDesignHeadline4TextBlock}" 
                       Visibility="{Binding HiddenCrosswordNameVisibility}"
                       Foreground="PapayaWhip" 
                       FontWeight="Bold"
                       HorizontalAlignment="Center"
                       FontSize="42"
                       Margin="10,8,10,0">
                <Run Text="{Binding CurrentEmployee.FullName}"></Run>
            </TextBlock>
        </StackPanel>
    </Grid>
</UserControl>
