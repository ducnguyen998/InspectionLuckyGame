<UserControl x:Class="InspectionLuckyGame.UI.Components.HiddenCard"
             xmlns="http://schemas.microsoft.com/winfx/2006/xaml/presentation"
             xmlns:x="http://schemas.microsoft.com/winfx/2006/xaml"
             xmlns:mc="http://schemas.openxmlformats.org/markup-compatibility/2006" 
             xmlns:d="http://schemas.microsoft.com/expression/blend/2008" 
             xmlns:local="clr-namespace:InspectionLuckyGame.UI.Components"
             mc:Ignorable="d">
    <CheckBox Width="160" Height="160" Cursor="Hand" Content="{Binding HiddenValue}" IsChecked="{Binding IsShow}">
        <CheckBox.Resources>
            <Style TargetType="CheckBox">
                <Style.Triggers>
                    <Trigger Property="IsChecked" Value="True">
                        <Setter Property="Template">
                            <Setter.Value>
                                <ControlTemplate TargetType="CheckBox">
                                    <Grid>
                                        <Grid.Background>
                                            <ImageBrush ImageSource="D:\SDV-Projects\InspectionLuckyGame\InspectionLuckyGame\Resources\box_2.png"/>
                                        </Grid.Background>
                                        <TextBlock Style="{StaticResource MaterialDesignHeadline2TextBlock}"
                                                   HorizontalAlignment="Center"
                                                   VerticalAlignment="Center"
                                                   Foreground="White"
                                                   FontWeight="SemiBold"
                                                   Text="{TemplateBinding Content}"/>
                                    </Grid>
                                </ControlTemplate>
                            </Setter.Value>
                        </Setter>
                    </Trigger>
                    <Trigger Property="IsChecked" Value="False">
                        <Setter Property="Template">
                            <Setter.Value>
                                <ControlTemplate>
                                    <Grid>
                                        <Grid.Background>
                                            <ImageBrush ImageSource="D:\SDV-Projects\InspectionLuckyGame\InspectionLuckyGame\Resources\box_3.png"/>
                                        </Grid.Background>
                                        <TextBlock Style="{StaticResource MaterialDesignHeadline2TextBlock}"
                                                   HorizontalAlignment="Center"
                                                   VerticalAlignment="Center"
                                                   Foreground="White"
                                                   FontWeight="SemiBold"
                                                   Text="?"/>
                                    </Grid>
                                </ControlTemplate>
                            </Setter.Value>
                        </Setter>
                    </Trigger>
                </Style.Triggers>
            </Style>
        </CheckBox.Resources>
        
    </CheckBox>
</UserControl>
