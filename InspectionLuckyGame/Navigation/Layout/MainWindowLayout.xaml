<UserControl x:Class="InspectionLuckyGame.Navigation.Layout.MainWindowLayout"
             xmlns="http://schemas.microsoft.com/winfx/2006/xaml/presentation"
             xmlns:x="http://schemas.microsoft.com/winfx/2006/xaml"
             xmlns:mc="http://schemas.openxmlformats.org/markup-compatibility/2006" 
             xmlns:d="http://schemas.microsoft.com/expression/blend/2008" 
             xmlns:local="clr-namespace:InspectionLuckyGame.Navigation.Layout"
             xmlns:materialDesign="http://materialdesigninxaml.net/winfx/xaml/themes"
             xmlns:ui="clr-namespace:InspectionLuckyGame.UI.View"
             mc:Ignorable="d" 
             d:DesignHeight="450" d:DesignWidth="800">
    <Grid>
        <Grid.RowDefinitions>
            <RowDefinition Height="Auto"/>
            <RowDefinition Height="*"/>
        </Grid.RowDefinitions>

        <Grid Grid.Row="1" Panel.ZIndex="9">
            <!--Top Bar-->
            <ContentPresenter Content="{Binding TopBar}"/>
        </Grid>
        <Grid Grid.Row="1" Panel.ZIndex="1">
            <materialDesign:DrawerHost
                BorderBrush="{DynamicResource MaterialDesignDivider}"
                BorderThickness="0"
                BottomDrawerBackground="{DynamicResource SecondaryHueLightBrush}"
                BottomDrawerCornerRadius="20 20 0 0"
                IsLeftDrawerOpen="{Binding TopBar.IsNavigationBarOpen}"
                >
                <materialDesign:DrawerHost.LeftDrawerContent>
                    <!--Navigation Bar-->
                    <ContentPresenter Content="{Binding NavigationBar}"/>
                </materialDesign:DrawerHost.LeftDrawerContent>
                <Grid
                    HorizontalAlignment="Center"
                    VerticalAlignment="Center">
                    
                    <!--Content Presenter-->
                    <ContentPresenter Content="{Binding MainContentPresenter}">
                        <ContentPresenter.Resources>
                            <DataTemplate DataType="{x:Type ui:HomeViewmodel}">
                                <ui:HomeView/>
                            </DataTemplate>
                            <DataTemplate DataType="{x:Type ui:PrizeConsolationFoam}">
                                <ui:SpinBasicView/>
                            </DataTemplate>
                            <DataTemplate DataType="{x:Type ui:PrizeConsolationHairDryer}">
                                <ui:SpinBasicView/>
                            </DataTemplate>
                            <DataTemplate DataType="{x:Type ui:PrizeThirdHeater}">
                                <ui:SpinAdvancedView/>
                            </DataTemplate>
                            <DataTemplate DataType="{x:Type ui:PrizeSecondSmartWatch}">
                                <ui:SpinAdvancedView/>
                            </DataTemplate>
                            <DataTemplate DataType="{x:Type ui:PrizeFirstRobotCleaner}">
                                <ui:SpinAdvancedView/>
                            </DataTemplate>
                            <DataTemplate DataType="{x:Type ui:WinnerViewmodel}">
                                <ui:WinnerView/>
                            </DataTemplate>
                        </ContentPresenter.Resources>
                    </ContentPresenter>
                </Grid>
            </materialDesign:DrawerHost>
        </Grid>
    </Grid>
</UserControl>
